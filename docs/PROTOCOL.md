# Mandate Protocol 0.1

Signed objects exclude `proof`, canonicalize with sorted keys, then Ed25519-sign.

Identifiers: `did:key` with Ed25519 multicodec `0xed01` and base58btc (`z…`).

A grant answers: who authorized, which agent, which scopes/budget, until when.
