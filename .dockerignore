# Allow-list: only what the image is built from. Keys, stores, .env files and
# anything else in the working tree never reach the build context.
*
!pyproject.toml
!README.md
!LICENSE
# Package sources only: a key or .env dropped inside mandate/ stays out too.
!mandate/*.py
!mandate/**/*.py
**/__pycache__
**/*.pyc
